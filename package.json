{
  "name": "ai-agent-local-memory",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter='*' build",
    "test": "bun run --filter='*' test",
    "typecheck": "bun run --filter='*' typecheck",
    "benchmark": "bun benchmark/runner.ts"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "^1.2.0",
    "better-sqlite3": "^12.10.0",
    "typescript": "^5.8.0"
  }
}
