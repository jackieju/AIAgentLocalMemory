{
  "id": "neural-context",
  "kind": "memory",
  "contracts": {
    "tools": [
      "neural_recall",
      "neural_remember",
      "neural_forget",
      "neural_note",
      "neural_status"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "storageDir": {
        "type": "string",
        "description": "Custom storage directory for graph.db and episodes"
      },
      "autoRecall": {
        "type": "boolean",
        "description": "Inject relevant memories before every AI turn"
      },
      "autoCapture": {
        "type": "boolean",
        "description": "Store conversations after every turn"
      },
      "maxRecallResults": {
        "type": "number",
        "minimum": 1,
        "maximum": 50
      },
      "debug": {
        "type": "boolean"
      }
    },
    "required": []
  },
  "uiHints": {
    "storageDir": {
      "label": "Storage Directory",
      "placeholder": "~/.local/share/ai-agent-local-memory",
      "help": "Where to store the neural graph database and episode files",
      "advanced": true
    },
    "autoRecall": {
      "label": "Auto-Recall",
      "help": "Inject relevant memories before every AI turn via spreading activation"
    },
    "autoCapture": {
      "label": "Auto-Capture",
      "help": "Automatically store conversations and build associative edges"
    },
    "maxRecallResults": {
      "label": "Max Recall Results",
      "placeholder": "10",
      "help": "Maximum memories injected into context per turn",
      "advanced": true
    },
    "debug": {
      "label": "Debug Logging",
      "help": "Enable verbose debug logs",
      "advanced": true
    }
  }
}
